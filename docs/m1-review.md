# M1 审查记录

固定 fixture 明确标为合成数学输入，URL 为保留示例域，不能视为真实网页。验证三向定价、亚洲 -0.25 五态 EV、负 EV 不被隐藏、冻结 DTO 和确定性数据输出。

报告排版函数只负责顺序输出，超过50行的 write_report 为文档章节编排；不包含足球计算。demo 超过50行为显式测试对象构建，不用于真实分析。后续真实输入通过独立导入模块进入 application。没有超过400行的文件。

渲染首次发现中文字体不可见及模板标题边线，需安装共享中文字体并移除模板边线后重新逐页检查。自动测试不能替代此步骤。

修复后4页已逐页打开确认：中文正常、标题无边线、表格未越界、无空白页，摘要等级/EV 与正文一致。30项全量测试、ruff、diff --check、compileall 通过。
