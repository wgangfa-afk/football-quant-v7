# M4 集成审查

角球与黄牌各自使用独立历史获得/送出统计及离散参数，负二项和Poisson计数基线都不复用进球λ。定价层有分布种类校验。V1不支持未经统一计分的红黄牌混合积分；这些报价明确PASS，不转换为黄牌盘口。

同一次公开研究的重复发现合并，开赛时间与历史统计冲突保留。历史报价仍保存在研究包和分析中，但不能被选为当前推荐。缺少进球历史时辅助模型独立运行。真实模式阻止测试证据，源码仅含明确合成fixture。

已加入输入有限性、赔率范围、DC边界、负EV评级、全市场结算、独立辅助分布、真实格式研究包到Word、hash篡改、来源片段、时间冲突、导入环和公共函数标注检查。pytest全量零网络；实际网页覆盖和预测有效性尚待M5，测试不构成实战验收。

## 大函数审查

无源文件超过400行。demo 为显式固定测试对象构建，约84行，保留直观测试数据便于审查；不用于真实分析。write_report 约54行为顺序章节编排，负责排版且没有计算概率。rate_group 为单一市场报价逐条核验、定价和择优，约65行；评级构造已拆为make_assessment，继续拆分会引入只转发参数的薄包装，因此保留。无God Object、数据库、资金账本或数据接口。

## 已知模型限制

衰减半衰期、融合权重、分级权重和3个百分点风险扣减为透明的V1政策，尚未回测验证。长期/近期模型共享样本，不能视为独立证据叠加；跨联赛没有桥接即PASS。重大人员变化无可核验调整模型时PASS。辅助模型暂假定两队计数独立，不量化比分状态或裁判因果影响。所有可给方向目前最高C，评级纯函数支持经未来证据校准后的S/A/B，但不会据测试样例提高实战等级。

M5须取得用户明确截止时间后进行真实公开网页研究，生成真实报告并创建审查PR。不得提前宣称真实数据链路已验收。

最终全量77项测试通过，ruff、git diff --check、compileall均通过。综合测试Word共7页，已逐页打开，确认中文、盘口/赔率格式、概率/EV、表格边界与分页，无空白页。报告明确测试性质；主要缺失项为测试包未提供的xG与裁判历史。
